var React = require('react')
var ReactDOM = require('react-dom');
var TaskManager = require('./components/TaskManager');

ReactDOM.render(
  <TaskManager />,
  document.getElementById( "container" )
);
